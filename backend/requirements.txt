# Sentinel backend (Phase 4/5) — self-sufficient for running the API image.
# Deliberately does NOT pull the training/streaming stack (xgboost,
# confluent-kafka, airflow): the serving container doesn't need them.
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
# Agent graph + RAG (imported by the API at request time)
langgraph>=0.2
langgraph-checkpoint-sqlite>=2.0
langchain-core>=0.3
haystack-ai>=2.0
numpy>=1.26
# LLM providers (used only when a key is set; router falls back to offline)
anthropic>=0.40
openai>=1.50
# Postgres (used only in SENTINEL_BACKEND_MODE=postgres; imported lazily)
psycopg[binary]>=3.2
